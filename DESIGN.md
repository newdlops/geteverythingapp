# 핫딜모아 Android 디자인 기준

## 목적과 범위

React Native `../getevapp`의 Android 네이티브 이식이다. 사용자는 여러 커뮤니티의 할인 상품을 훑고 원문을 열거나 포럼에서 정보를 나눈다. 기존 보라색 브랜드, 한국어, 5개 탭을 유지한다. 원본의 숫자 캐러셀·광고 자리 표시자·더미 상품은 실제 콘텐츠처럼 이식하지 않는다.

## 화면과 흐름

- 메인화면: 검색 → 핫딜 목록 → 외부 원문. 상품명·가격 우선, 출처·시간·반응 수 후순위. 새로고침, 커서 페이지 추가, 맨 위로 이동.
- 포럼: 공지 우선 목록 → HTML 본문·댓글 → 로그인 후 댓글/글 등록. 전송 실패 시 입력 보존.
- 관심상품: 로그인 안내 및 저장한 상품. 저장은 기기 내 계정별이며 서버 동기화를 약속하지 않는다.
- 알림설정: 원본에 실제 알림 기능이 없으므로 지원 상태를 정확히 안내한다. 작동하지 않는 스위치를 만들지 않는다.
- 내계정: 카카오 로그인·회원가입·로그아웃. 실패와 취소를 가입 필요 상태와 구분한다.

## 시각 언어

- 브랜드 `#6C28FE`, 흰색 표면, 밝은 회색 배경. 보라색 상단/하단 탐색 유지. 선택 탭은 아이콘 배경과 굵기로 구분.
- Android 시스템 글꼴과 한국어 fallback. 본문 16sp, 보조 12–14sp, 제목 20–24sp. 시스템 글자 크기를 존중한다.
- 텍스트 대비 4.5:1, 아이콘 3:1 이상. 가격/오류는 의미별 색을 사용하고 텍스트도 함께 표시.
- 4/8dp 간격, 기본 여백 16dp, 섹션 간격 24dp, 모서리 8–12dp. 목록은 밀도 중간, 불필요한 그림자/중첩 카드 금지.
- Compose Material 3의 버튼·입력·대화상자와 단일 Material 아이콘 계열. 원본의 브랜드를 시스템 동적 색상으로 덮지 않는다.
- 최소 360dp 폭. 고정 픽셀 너비와 절대 배치를 피하고 긴 상품명은 최대 3줄, 메타데이터는 줄바꿈한다. 600dp 이상은 좌측 탐색 rail을 사용하고 읽기 폭은 720dp 이내 중앙 정렬한다. 큰 글꼴에서는 하단 탭 이름을 짧게 표시하고 접근성 이름은 유지한다.
- 상태/탐색 막대와 키보드 inset 적용. 터치 영역 최소 48dp. 의미 있는 아이콘에 접근성 이름 제공.
- 기본 native ripple/전환만 사용. 자동 재생·불필요한 이동 애니메이션 없음. 시스템 애니메이션 설정을 따른다.

## 필수 상태

초기 로딩, 새로고침, 다음 페이지 로딩, 빈 목록, 연결 오류와 재시도, 페이지 추가 실패, 이미지 없음, 마지막 페이지, 선택 탭, 등록 중/완료/실패, 비로그인, 만료된 세션, 카카오 설정 누락, 입력 검증, 긴 제목/URL/계정 이름, 큰 글꼴, 키보드 표시, 뒤로가기와 작성 취소를 다룬다.

## 수용 기준

1. Metro/Node/React Native 없이 Gradle에서 APK를 빌드한다.
2. 실제 API 핫딜 목록·다음 페이지·새로고침·외부 링크와 포럼 목록/상세가 동작한다.
3. 쓰기 요청 중 중복 제출을 막고 실패한 입력을 보존한다. 인증 토큰은 Android Keystore를 이용해 암호화 저장한다.
4. 작은 휴대전화·태블릿·가로 화면에서 시스템 바 겹침/가로 넘침을 확인한다. 큰 글꼴과 네트워크 실패도 검사한다.
5. 빌드/테스트와 에뮬레이터 시각 검증을 각각 기록한다. 계정이 필요한 검증은 수행 여부를 구분한다.

UI UX Pro Max 검색은 참고 자료로 사용했다. 새 색상/랜딩 페이지 스타일 제안은 기존 제품과 맞지 않아 채택하지 않았다. 웹 UI가 아니므로 웹 브라우저·React 감사 대신 Android 에뮬레이터와 Compose semantics를 검증한다.
